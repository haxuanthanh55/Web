**1.SCOPE(Phạm vi biến)**(Global Scope: Biến khai báo ngoài cùng, mọi nơi gọi được.)**const TEN\_CONG\_TY = "ABC Hotel";****function xemThongTin() { console.log(TEN\_CONG\_TY); }**(Function Scope: Biến trong hàm, ra ngoài hàm là mất.)**(function tinhLuong() {**&#x20;   **let tienThuong = 500000;****}**// console.log(tienThuong); -> Báo lỗi ngay vì biến đã "chết" khi hàm chạy xong.Block Scope { }: Biến trong ngoặc nhọn của if hoặc vòng lặp.**if (true) {**&#x20;   **let maGiamGia = "SALE10";****}**// console.log(maGiamGia); -> Lỗi, không gọi được ra ngoài dấu { }**2.Lỗi thường gặp \& Stack Trace**(TypeError (Sai kiểu dữ liệu - Lỗi phổ biến nhất): Cố dùng hàm của mảng trên một số hoặc chữ.)**let tuoi = 20;****tuoi.map(x => x \* 2);****3. Try / Catch (Bẫy lỗi)****try {**&#x20;   // Cố tình ép hệ thống chạy một lệnh sai lầm&#x20;   **JSON.parse("chuỗi không phải dạng JSON chuẩn");****} catch (error) {**&#x20;   // Web không sập, mà nhảy vào đây để xử lý hậu quả&#x20;   **console.log("Hệ thống phát hiện lỗi phân tích dữ liệu, đã được bọc lại an toàn!");****}****4. Modules (Tách file import/export)**(Chia nhỏ code ra nhiều file để quản lý.)File math.js (Nơi cho đi - Export):**export const congHaiSo = (a, b) => a + b;**File main.js (Nơi nhận về - Import):**import { congHaiSo } from './math.js';****console.log(congHaiSo(5, 10));**// Kết quả: 15